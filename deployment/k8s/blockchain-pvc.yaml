apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blockchain-data
  namespace: isa-cloud-staging
  labels:
    app: blockchain-node
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
